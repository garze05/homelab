services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped

    expose:
      - "3001"

    dns:
      - 192.168.1.55

    volumes:
      - /srv/docker/uptime-kuma/data:/app/data

    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
